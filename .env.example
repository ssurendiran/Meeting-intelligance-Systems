OPENAI_API_KEY=########################
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=meeting_chunks
# Prompt version (app/prompts/v1/, v2/, ...)
PROMPT_VERSION=v1

# Optional: cost per 1K tokens (vendor can change). Defaults are set for gpt-4o-mini and text-embedding-3-small.
# Override only if you use different models or pricing:
# COST_CHAT_INPUT_PER_1K=0.00015
# COST_CHAT_OUTPUT_PER_1K=0.0006
# COST_EMBEDDING_PER_1K=0.00002
